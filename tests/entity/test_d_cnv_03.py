"""RED skeleton — Track B: D-CNV-03 (feet→yard via meter)."""

import pytest


def test_d_cnv_03_feet_to_yard_via_meter() -> None:
    # Given: feet 값
    # When: convert_all (feet → yard) 호출
    # Then: meter 경유 환산과 일치
    pytest.fail("RED: D-CNV-03 — feet→yard meter 경유 환산 미구현, 의도적 실패")
