"""RED skeleton — Track B: D-REG-01 (register cubit)."""

import pytest


def test_d_reg_01_register_cubit_convertible() -> None:
    # Given: cubit = 0.4572 m
    # When: registry.register("cubit", 0.4572) 후 변환
    # Then: 변환 가능
    pytest.fail("RED: D-REG-01 — cubit 등록·변환 미구현, 의도적 실패")
